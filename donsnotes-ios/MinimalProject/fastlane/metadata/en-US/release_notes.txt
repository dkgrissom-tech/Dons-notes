Build 109 — persistent diagnostic log + upload logging.

The diagnostic log used to reset every time you tapped record — so if you got an empty email and started a fresh recording to capture the log, the failing session's evidence was already gone.

Fix: the log now persists across recordings. Each new session shows up under a clear "SESSION BOUNDARY" line so you can tell them apart.

Also added: the upload path now logs the transcript character counts and which branch it takes. This is the smoking gun for empty-email diagnosis.

No behavior changes to recording, upload, or email.
