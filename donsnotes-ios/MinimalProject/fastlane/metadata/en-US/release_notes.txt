Build 107 — speech-recognizer visibility.

Recording behavior is unchanged from Build 106. This build adds diagnostic logs so we can see whether Apple's speech recognizer is producing results.

If your next recording ends with an empty email, long-press the orb 3 seconds and share the diagnostic log — the SPEECH entries will show exactly where the transcription pipeline died.
